services:
  server:
    build:
      context: .
      args:
        NEXT_PUBLIC_IPRO_FIX_BASE_URL: ${NEXT_PUBLIC_IPRO_FIX_BASE_URL:-http://localhost:4000/api/}
    ports:
      - 3000:3000
    env_file: ${ENV_FILE:-.env.local}
    environment:
      AUTH_TRUST_HOST: "true"
